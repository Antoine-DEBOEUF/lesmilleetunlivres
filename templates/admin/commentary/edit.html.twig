{% extends "base.html.twig" %}

{% block title %}Modifier un avis
	{{parent()}}
{% endblock %}

{% block body %}
	<h1>Modifier un avis</h1>
	<a href="{{path('books.details', {id: book.id})}}">Retour</a>
	{{form_start(form, {'attr':{'class':'formAdmin'}})}}
	{{form_widget(form)}}
	<button type="submit">Modifier</button>
	{{form_end(form)}}

{% endblock %}
