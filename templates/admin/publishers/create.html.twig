{% extends "base.html.twig" %}

{% block title %}Editeur
	{{parent()}}
{% endblock %}

{% block body %}
	<h1>Création d'une fiche éditeur</h1>

	<a href="{{path('admin.publishers.index')}}">Retour à l'index</a>

	<div>
		{{form_start(form)}}
		{{form_widget(form)}}
		<button type="submit">Créer la fiche</button>
		{{form_end(form)}}

	</div>

{% endblock %}
