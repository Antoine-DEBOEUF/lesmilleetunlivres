{% extends "base.html.twig" %}

{% block title %}
	Modifier une fiche livre
	{{parent()}}
{% endblock %}

{% block body %}
	<h1>Modification d'une fiche livre</h1>

	<a href="{{path('admin.books.index')}}">Retour à la liste</a>

	{{form_start(form)}}
	{{form_widget(form)}}
	<button type="submit">Enregistrer les modifications</button>
	{{form_end(form)}}

{% endblock %}
